---
description: 新增已完成任務（回溯紀錄工作）
---

On `/add_done <description> [flags same as /add_task]`:

Same flow as `/add_task` (require list, same tag/priority/PR handling, confirm card),
but when creating pass `--status done` to `tasks create`.

Reply: `✅ 已登記完成的任務：<url>`
